# This project currently keeps local runtime configuration in
# src/main/resources/application.yml instead of .env.
#
# If env-based configuration is reintroduced later, document the required
# variables here.
